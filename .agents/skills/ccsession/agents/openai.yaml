interface:
  display_name: "ccsession"
  short_description: "Find and resume local agent sessions"
  default_prompt: "Use $ccsession to find the prior agent session for this work and recommend whether to resume it."
